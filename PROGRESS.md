# 项目进度记录

> 记录各子任务进展、结论与产物，随实验更新。最新进度在最上。

---

## 阶段 1：三维场景拓扑识别（2026.09 – 2026.11）

### 子任务 A：合成点云 PH 参数扫描与建议 ✅ 已完成

**目标**：对 5 种合成对象（circle/disk/torus/sphere/porous_block），扫描子采样比例，给出复形参数选择建议，回应申报书"计算复杂度优化"拟解决问题。

**关键改进（本次重跑）**：
- 修正判据 bug：原"WD 相对基准比值"判据因基准 WD=0 全部失效，改为**绝对 WD 阈值 = 点云直径 × 10%**
- 新增多空腔结构生成器 `generate_porous_block`（立方体挖 3 球腔）

**结论**（5 对象 × 4 子采样 × 各维度 H0/H1/H2）：
| 对象 | 建议子采样 | 判据（10% 直径阈值） |
|------|-----------|----------------------|
| circle | ≥ 100% | WD < 0.229（直径 2.29） |
| disk | ≥ 100% | WD < 0.214（直径 2.14） |
| torus | ≥ 100% | WD < 0.613（直径 6.13） |
| sphere | ≥ 50% | WD < 0.223（直径 2.23） |
| porous_block | ≥ 100% | WD < 0.320（直径 3.20） |

- circle/disk/torus 因空间尺度小、阈值严，建议保持高密度；sphere 在 50% 子采样下 WD 已达标。
- 计算时间随子采样比例立方下降（sphere 100% 的 H2 约 36s，10% 仅 0.016s）。

**产物**：`results/exp1a_param_scan/`（5 张图 + summary.csv）、`src/experiment_1a_param_scan.py`

**提交**：`8157b61`

---

### 子任务 B：拓扑特征 vs 几何特征对比 ✅ 已完成

**目标**：同一批 5 类合成对象点云，分别提取拓扑特征与传统几何特征，用相同分类器比较，验证申报书"拓扑特征优于传统几何特征"。

**实验设计**：
- 240 样本 = 5 对象 × 4 噪声（0/5/10/20%）× 4 旋转（0/45/90/180°）× 3 重复
- **拓扑特征（25 维）**：WD/BD（与单位圆基准比，每维 3 个）+ top-k 持久性（H1，10）+ 持久点统计（每维点数/均值/最大，9）
- **几何特征（18 维）**：PCA 主轴/形状描述子（7）+ 凸包（4）+ 密度/最近邻（7）
- 分类器：SVM(RBF) + RandomForest，5 折重复取均±std

**结果（公平版，SO(3) 真随机旋转）**：
| 特征 | SVM | RF | 平均 |
|------|-----|-----|------|
| **拓扑特征（25维）** | **0.9167** | **0.9056** | **91.11%** |
| **几何特征（67维）** | 0.6861 | 0.7417 | 71.39% |
| **几何特征（PCA降25维）** | 0.5889 | 0.6806 | 63.48% |

**结论**：改用真随机 SO(3) 旋转后，拓扑特征优势反而扩大（91.11% vs 71.39%，差 19.7 个百分点）。这**决定性证明拓扑特征基于距离、天然旋转不变**，而几何特征（法线/凸包/FPFH）对随机旋转敏感。PCA 同维对比下几何特征更差（63%），印证拓扑特征"低维高信息量"优势。这是申报书核心主张的严谨支撑。

**公平性措施**：
- SO(3) 真随机旋转（QR 分解，非单一 z 轴）
- 几何特征 PCA 降到同 25 维对照
- 三组特征各出一张混淆矩阵

**技术备注**：
- open3d 0.19 的 `compute_fpfh_feature` 对合成点云返回空数组（bug），已加保护并改用更可靠的纯 numpy/scipy 几何特征。
- PH 计算（ripser）纯 CPU；GPU（RTX 5060）留给阶段 2 深度学习。

**产物**：`results/exp1b_topo_vs_geo/`（X_topo/X_geo/labels.npy + summary.csv + comparison.png）、`src/experiment_1b_topo_vs_geo.py`

**提交**：`5826e44`（初版，numpy 弱特征）、`待更新`（强版 18 维）

---

### 子任务 C：ModelNet10 三维真实物体分类 🔄 进行中

**目标**：真实三维数据上验证拓扑特征分类能力，冲刺 85% 准确率。

**进度**：
- ModelNet10 数据已下载解压（10 类 4899 个 .off，train/test 官方划分）
- 已修通 pipeline：trimesh 加载 .off → 表面采样点云 → 拓扑/几何/融合特征提取
- 性能优化：512 点单样本 8.3s → 降到 256 点 3.2s（全量 ~3.5 小时可行）
- pilot 试点（每类 20 个，共 200 个）进行中
- 实验设计：拓扑(25维) / 几何(67维) / 融合 三组对比 + GroupKFold 防泄漏 + SVM/RF

**待办**：
- [ ] pilot 出结果，验证端到端
- [ ] 全量训练集特征提取（~4000 个，~3.5 小时）
- [ ] 三组对比 + 85% 目标评估

---

### 阶段 2A：高维时序拓扑表征（合成验证阶段）✅

**目标**：验证"延迟嵌入 → PH"能区分不同动力学类型，为阶段 2 真实时序实证打基础。

**成果**：
- 合成时序生成器（周期/拟周期/混沌Rössler+Lorenz/白噪声/AR1）
- Takens 延迟嵌入 + 嵌入参数启发式（自相关τ/互信息τ/FNN m）
- 参数扫描（m∈{3,4,5,6}, τ∈{1,2,4,8}）：
  - **周期 H1=1 个强持久环**（完美符合极限环预期）
  - 混沌数十环、噪声数百环
- **判据优化**（统一 m=4,τ=4 比较 6 种判据）：
  - 周期vs噪声：全部判据可区分（区分度 31-∞）
  - **混沌vs噪声（难任务）：比例判据（环数>30%最大持久度）最优，区分度 9.12**；top1/均值次之（4.53）
  - 绝对阈值在混沌vs噪声失效（尺度不匹配）

**核心结论**：合成验证证明——周期信号产生 1 个强持久环，混沌产生数十个环，噪声产生数百个环；比例判据是最优判据，能区分混沌与噪声（区分度 9.12）。拓扑特征可可靠区分周期/混沌/噪声动力学。

**产物**：`src/experiment_2a_*.py`、`results/exp2a_synth_scan/`、`results/exp2a_criterion/`

---

### 阶段 2A：真实时序实证（ECG200）🔄 待启动

**计划**：UCR ECG200 心拍二分类（正常 vs 心肌梗死），复用延迟嵌入 + 比例判据拓扑特征，三组对比（拓扑/时频/融合）+ SVM/RF。

---

### 阶段 2B：向量化研究（进行中）

#### 任务 1：噪声稳定性对比 ✅ 已完成
- 环面点云(500点)，σ∈{0.01,0.05,0.1,0.2}，10次重复
- 四种向量化：top-k(10D) / PI(1600D) / PL(500D) / Betti(50D)，统一固定范围[0,1]
- **结果（σ=0.2从好到差）**：
  1. **PI: 0.0848**（最稳定——高斯核平滑噪声）
  2. **Betti: 0.2392**
  3. **Top-K: 0.3365**（排序敏感）
  4. **PL: 0.6517**（最差——景观对位置变化极敏感）
- **关键发现**：PI 的平滑性反而是噪声稳定性优势，纠正"top-k 最稳定"的直觉
- 产物：`src/experiment_2b_vectorization_stability.py`、`results/exp2b_vectorization/`

#### 任务 2：旋转/扰动连续性 ✅ 已完成
- 环面点云 SO(3) 旋转（5°/15°/30°/45°），10次重复
- **关键发现**：四种向量化的变化率与旋转角度无关——Ripser 距离矩阵旋转不变，持久图完全不变，变化仅来自数值精度
- 结果（所有角度相同）：PI(0.0123) > Betti(0.0285) > Top-K(0.0553) > PL(0.1649)
- 证明拓扑特征天然旋转不变，实验可简化

#### 任务 3：ECG200 上的向量化效果 ✅ 已完成
- 四种向量化在 ECG200 分类（RF，5折CV + 固定test）
- **结果（test 准确率）**：Top-K(74%) > PI(72%)=PL(72%) > Betti(68%)
- **关键发现**：
  1. 低维 Top-K(10D) 在小数据集最有效，高维 PI/PL 即使 PCA 降维也不占优
  2. PL 的 CV 方差最小(0.058)最稳定，但 test 不占优
  3. **稳定性与分类效果不完全正相关**——任务1中 PI 抗噪最好，但任务3中 PI 分类不如 Top-K（平滑性抹去判别细节）
  4. 所有纯拓扑向量化(68-74%)远低于融合(85%)，证实拓扑需与时频融合

---

### 阶段 2B 总结：向量化研究的三个核心结论

1. **噪声稳定性**（任务1）：PI(0.085) > Betti(0.239) > Top-K(0.337) >> PL(0.652)——平滑性抗噪
2. **旋转连续性**（任务2）：所有向量化旋转不变（Ripser 距离矩阵性质），证明拓扑特征天然旋转不变
3. **分类效果**（任务3）：Top-K(74%) > PI/PL(72%) > Betti(68%)——低维在小数据集更有效

**核心洞察**：**稳定性 ≠ 判别力**。PI 抗噪最好但分类不如 Top-K；PL 最连续但 CV 稳 test 弱。向量化选择需权衡"鲁棒性"与"判别力"，没有单一最优——这为阶段 2C（深度学习融合）提供了理论铺垫：应根据任务特性选择向量化，或用可学习的方式（PersLay）自适应组合。

**创新点方向**：传统向量化是固定的 PD→Banach 映射，本研究发现其稳定性与判别力的权衡，提示**可学习向量化**（如 PersLay 的可微过滤）可能是更优解——这正是阶段 2C 的方向。

---

### 阶段 2C：可学习向量化（PersLay-lite）✅ 已完成（四轮实验，三数据集）

**设计**：简化版 PersLay——可学习权重 w=σ(a·pers+b) + 可学习带宽高斯核，PD→20×20网格(400D)→MLP；
PD 预计算缓存（ripser 不可微，PersLay 标准做法）。协议：5折CV选epoch，test 只评一次。

**第 1 轮 ECG200**（100 train）：纯拓扑 CV 81%/test 74%；融合 CV 82%/test 78%
（基线固定融合 85%）。问题：学习参数几乎不动（数据饥饿）、fold 方差大。

**第 2 轮 定向修复**：分离 LR（PersLay 10×）纯拓扑 test 72%、融合 test 79%；
**混合方案（PersLay 特征→RF）test 81%** 最佳。参数仍只微动——100 样本喂不饱向量化层。

**第 3 轮 FordA**（3601 train，×36；降采样 100 点，PH 8 进程仅 3s）：

| 方案 | CV | Test |
|------|-----|------|
| 固定 Top-K+RF | 52.3% | 50.3%（随机） |
| **固定 Top-K+时频+RF** | 84.6% | **85.2%** 🏆 |
| PersLay 纯拓扑 | 62.3% | 61.7% |
| **PersLay+时频 MLP** | 84.3%±0.9% | **83.6%** |
| PersLay→RF 混合 | 79.5% | 81.4% |

**数据量假设部分成立**：差距从 −7pp 缩至 −1.6pp；参数真的动了（a 2.0→1.16, b −1.0→−1.38,
σ 0.10→0.048，学到"更尖锐的核+更低阈值"）；CV 方差 ±9.7%→±0.9%。
未反超原因：FordA 判别信息在时频域（纯拓扑仅 50-62%），天花板效应。

**核心结论**：**可学习向量化受"数据量 × 任务拓扑相关性"双重制约**——数据充足(>1000)且任务含
拓扑信息时可逼近并自适应替代手工向量化；小样本下手工特征+RF 仍最优；两数据集一致显示
融合时频特征是关键。与 2B"稳定性≠判别力"共同构成向量化研究完整证据链。

**第 4 轮 ECG5000（5 类心律多分类，500 train / 4500 test）——任务相关性维度直接验证**：

| 方案 | CV | Test |
|------|-----|------|
| 固定 Top-K+RF | 85.8% | 87.5% |
| 固定 Top-K+时频+RF | 92.8% | 91.4% |
| PersLay 纯拓扑 MLP | 91.2% | 89.8% |
| **PersLay特征+时频→RF 混合** | **93.2%** | **92.0%** 🏆 |

- **可学习向量化首次反超固定基线**（混合 92.0% > 固定融合 91.4%）
- 纯拓扑 87.5–89.8% >> FordA 的 50–62%——证实"任务拓扑相关性"维度（心律形态编码在拓扑里）
- 印证双重门槛：ECG5000 同时有足够样本 + 强拓扑相关性，是唯一胜出场景
- 异常记录：PersLay+时频 **MLP** 融合发散(58.4%)，但同模型抽特征喂 **RF** 达 92%——
  问题在 MLP 头不在表征，再次印证"表征器+RF"是最稳健配置

**第 4b 轮 稳定性确认（5 种子 + 统计检验 + 类别不平衡诊断，诚实修正）**：
- 多种子：H1 PersLay混合 **92.0%±0.07%** vs 固定 91.36%±0.04%，**5/5 种子胜出**
- 检验：配对 t p<0.0001（显著），Wilcoxon p=0.0625（边缘 n=5），McNemar p=0.041
- **不平衡诊断（关键修正）**：ECG5000 不平衡比 146×，92% accuracy 由多数类(N+S=93.7%)撑起；
  少数类 F/Q recall≈0（固定与可学习都一样）；**macro-F1 口径固定基线反而占优（0.468 vs 0.439）**
- 表述修正：accuracy 口径应说"**追平/略优**"而非"反超"；平衡指标口径不敌固定基线
- 教训：146× 不平衡 + 小 train 数据集，整体 accuracy 是误导性指标，必须附 per-class 指标

**最终结论（三数据集）**：可学习向量化的优势需"数据量 + 任务拓扑相关性"双重门槛；
最优用法是 PersLay 表征 + RF（ECG200 81% / FordA 81.4% / ECG5000 92.0% 均为各集可学习方案最佳）；
ECG5000 上 accuracy 口径略优(+0.6pp)但平衡指标口径不敌固定——条件性结论比"普遍更优"更诚实。

**产物**：`src/perslay_lite.py`、`src/experiment_2c_*.py`、`results/exp2c_perslay/`、
`results/exp2c_forda/`、`results/exp2c_ecg5000/`、`STAGE2C_SUMMARY.md`（含创新点表述建议 + σ 极限插值理论观察）

---

## 下一步

- **带 2A/2B/2C 图表找导师对齐创新点**（时机已到：2A 合成+ECG200 达标 85%、2B 三结论、2C 数据量检验）
- 可选加码：拓扑相关性更强的时序集验证"任务相关性"维度另一半（预期可学习向量化反超）
- 结题材料：阶段 1（STAGE1_SUMMARY）+ 阶段 2（STAGE2AB_SUMMARY + STAGE2C_SUMMARY）已齐
- 团队分工后期处理（用户负责）