# 阶段 2C 总结：可学习向量化（PersLay-lite）与深度学习融合

> SRTP「基于持续同调的拓扑特征识别及其应用」· 阶段 2 收官
> 2026.09.10 · 四轮实验全部提交 git
> 前置依据：阶段 2B 核心洞察「稳定性 ≠ 判别力」——固定向量化没有单一最优

---

## 一、动机与设计

2B 发现：PI 抗噪最好但分类不如 Top-K，PL 最连续但 test 不占优——**固定向量化是手工选定的
PD→Banach 映射，各有短板**。2C 的问题：让网络自己学这个映射，能否突破固定向量化的上限？

### PersLay-lite 模块（`src/perslay_lite.py`）

简化版 PersLay（Carrière et al., AISTATS 2020），两个可学习部件：

| 部件 | 固定向量化（PI） | PersLay-lite |
|------|------------------|--------------|
| 点权重 | 手工设定 | **w_i = sigmoid(a·pers_i + b)**，a、b 可学习 |
| 核带宽 | 手工 σ | **全局可学习 σ**（log 参数化保正性） |

PD 点 (birth, pers) 经加权高斯核投影到 G×G 网格（G=20，400D）→ LayerNorm → MLP 头。

**不可微边界**：ripser 的 PH 计算不可微 → PD 预计算缓存（.npz），可学习部分从 PD 之后开始
（PersLay 原论文标准做法）。

**权重函数的意义**：w= sigmoid(a·pers+b) 正是 2A 手工调的"比例判据"（持久度>30%最大值）的
可微版——网络自己决定"多持久才算显著"，学习结果可与手工判据直接对照。

---

## 二、三轮实验

### 第 1 轮：ECG200 首轮验证（commit 0812415）

数据：ECG200（100 train / 100 test），Takens m=5, τ=4，H1 持续图。
协议：train 5折CV（早停 + 记录最优 epoch）→ epoch 取 CV 中位数 → **test 只评一次**。

| 配置 | CV | Test |
|------|-----|------|
| PersLay 纯拓扑 (400D→MLP) | 81.0% ±9.7% | 74.0% |
| PersLay+时频 (411D→MLP) | 82.0% ±7.5% | 78.0% |
| 基线 Top-K+RF（2B） | 72% | 74% |
| 基线 固定融合28D+RF(50)（2A） | — | **85%** |

**暴露的问题**：
1. 学习参数几乎没动（a 2.00→2.00, b −1.00→−0.99, σ 0.100→0.101）——**向量化层"数据饥饿"**
2. fold 间方差极大（val 0.65–0.95），fold1 甚至 best_epoch=2 就早停
3. CV 81% 明显高于 Top-K 的 72%（+9pp），但 test 只有 74%——CV→test 掉 7pp

### 第 2 轮：定向修复（commit c4705c7）

针对"参数没动"和"MLP 不敌 RF"两个假设做三个变体：

| 变体 | 改动 | CV | Test |
|------|------|-----|------|
| A | PersLay 层单独 10× 学习率（1e-2），纯拓扑 | 81.0% | 72.0% |
| A2 | 分离 LR，融合时频 | **84.0%** ±6.6% | 79.0% |
| **B** | **PersLay 特征(400D)+时频(11D) → RF(50) 混合** | 83.0% ±5.1% | **81.0%** |

**结论**：分离 LR 让参数确实移动了一点（σ 0.100→0.114），但幅度仍微小；
混合方案 B（可学习向量化 + 2A 证明的小数据强分类器）test 最佳 81%，仍距 85% 差 4pp。
**100 条训练样本喂不饱可学习向量化层**——这是数据量问题，不是模型问题。

### 第 3 轮：FordA 数据量假设检验（commit e1903b5）

数据：**FordA（3601 train / 1320 test，ECG200 的 36 倍）**，发动机噪声检测二分类，
499 点序列 → 降采样 100 点（与 ECG200 96 点协议对齐）。
PH 缓存：8 进程并行，4927 样本仅 **3 秒**（降采样后嵌入 84 点，ripser 极快）。

| 方案 | CV | Test |
|------|-----|------|
| 固定 Top-K(10D)+RF | 52.3% | 50.3%（随机水平） |
| **固定 Top-K+时频(21D)+RF** | 84.6% | **85.2%** 🏆 |
| PersLay 纯拓扑 MLP | 62.3% | 61.7% |
| **PersLay+时频 MLP** | 84.3% ±0.9% | **83.6%** |
| PersLay特征+时频 → RF 混合 | 79.5% | 81.4% |

**数据量假设：部分成立**

支持的证据：
1. **差距从 −7pp（ECG200: 81% vs 85%）缩至 −1.6pp（FordA: 83.6% vs 85.2%）**
2. **PersLay 参数这次真的动了**：a 2.0→**1.16**、b −1.0→**−1.38**、σ 0.10→**0.048**
   ——网络学到了"更尖锐的核（σ 减半）+ 更低的显著性阈值"，向量化层真正参与学习
3. **CV 方差从 ±9.7% 降到 ±0.9%**——大数据量下训练完全稳定

未反超的原因（诚实解释）：
- FordA 是发动机噪声信号，**判别信息主要在时频域**：纯拓扑特征只有 50–62%
  （Top-K 甚至到随机水平），拓扑在这类信号上天然弱
- 时频基线本身已达 85.2%，**天花板效应**——可学习向量化没有额外的拓扑信息可挖

### 第 4 轮：ECG5000 任务拓扑相关性检验（多分类，commit 见下）

第 3 轮留下的疑问：FordA 未反超，到底是"可学习向量化本身不行"，还是"FordA 这个任务
拓扑信息太少"？为分离这个变量，选 **ECG5000**——5 类心律多分类（正常 + 4 种异常节律），
心拍形态差异大，**拓扑相关性应显著强于 FordA**；且 test 有 4500 条，评估稳定。

数据：ECG5000（500 train / 4500 test，5 类，140 点心拍序列，官方固定划分）。
协议：Takens m=5, τ=4，H1 持续图，train 5折CV 选 epoch，test 只评一次。
（`perslay_lite.TopoClassifier` 增加 `num_classes` 支持多分类，向后兼容二分类）

| 方案 | CV | Test |
|------|-----|------|
| 固定 Top-K(10D)+RF | 85.8% | 87.5% |
| 固定 Top-K+时频(21D)+RF | 92.8% | 91.4% |
| PersLay 纯拓扑 MLP | 91.2% | **89.8%** |
| PersLay+时频 MLP | 65.0% | 58.4%（融合训练发散，见下） |
| **PersLay特征+时频 → RF 混合** | **93.2%** | **92.0%** 🏆 |

**假设成立**：
1. **可学习向量化首次反超固定基线**：混合方案 92.0% > 固定融合 91.4%（+0.6pp），
   PersLay 纯拓扑 89.8% 也逼近——这是三个数据集里可学习向量化唯一胜出的一次
2. **纯拓扑准确率 87.5–89.8% >> FordA 的 50–62%**——直接证实"任务拓扑相关性"维度：
   心律的形态差异确实编码在拓扑里，可学习向量化有信息可挖
3. PersLay 参数显著学习（a 2.0→2.34, b −1.0→−1.39, σ 0.10→0.135）

**一个诚实记录的异常**：PersLay+时频 **MLP** 融合在 5 类多分类下训练发散
（test 仅 58.4%，fold2-5 在 ep1-9 早停），但**同一个训练好的 PersLay 层抽特征喂 RF
却达 92.0%**。这说明问题不在向量化表征（表征是好的），而在 MLP 头在多分类 + 高维拼接
下的优化不稳定——与 2A/2C 一贯的"小数据 MLP 不敌 RF"发现一致。

### 第 4b 轮：稳定性确认与诚实修正（5 种子 + McNemar + 不平衡诊断）

对上述 +0.6pp"反超"做统计确认（`src/experiment_2c_stability_ecg5000.py`）：

**多种子稳定性**（5 seeds，H1=PersLay混合 vs 固定 Top-K+时频+RF）：

| 方案 | Test Acc（均值±std） |
|------|---------------------|
| 固定 Top-K+TF+RF | 91.36% ± 0.04% |
| H1 PersLay混合+RF | **91.99% ± 0.07%**（5/5 种子胜出） |
| H2 PersLay(纯拓扑配置)+RF | 91.81% ± 0.08%（5/5 种子胜出） |
| H1+RF class-balanced | 92.03% ± 0.12% |

统计检验：配对 t 检验 p<0.0001（显著）；Wilcoxon p=0.0625（边缘，n=5）；
McNemar（seed42，4500 test）p=0.041。
→ **"反超"方向一致且大概率真实，但幅度小（+0.6pp），稳妥表述为"略优/追平"。**

**类别不平衡诊断（重要修正）**：ECG5000 五类极不均衡（N 58.4% / S 35.3% / V 1.9% / F 3.9% / Q 0.5%，
不平衡比 146×）。per-class recall（seed42）显示：

| 类别 | 固定 | H1 PersLay | H2 PersLay |
|------|------|-----------|-----------|
| N(正常, n=2627) | 0.989 | 0.984 | 0.986 |
| S(室上性, n=1590) | 0.936 | 0.966 | 0.956 |
| V(室性, n=86) | 0.279 | 0.233 | 0.279 |
| F(融合, n=175) | 0.017 | 0.000 | 0.006 |
| Q(未知, n=22) | 0.000 | 0.000 | 0.000 |

- **92% 的 accuracy 几乎完全由多数类（N+S 占 93.7%）撑起**；少数类 F/Q 的 recall ≈ 0，
  所有方案（固定与可学习）都一样——这是类别不平衡 + train 仅 10/2 个样本的固有困难，不是方法失败
- **macro-F1 口径下固定基线反而更高**：固定 0.468 > H1 0.439 / H2 0.439；
  balanced accuracy 同样固定占优（0.447 vs 0.430/0.433）
- RF class_weight='balanced' 只把 accuracy 提到 92.03%，macro-F1 未实质改善（少数类 train 样本太少）

**修正后的第 4 轮结论**：
1. accuracy 口径：PersLay 混合 **略优于**（+0.6pp，5/5 种子一致，McNemar p=0.041）固定基线——
   说"追平/略优"比"反超"更稳妥
2. **平衡指标（macro-F1/balAcc）口径：固定基线仍占优**——可学习向量化在少数类上没有增益
3. "任务拓扑相关性"维度的证据不受影响（纯拓扑 87-90% vs FordA 50-62% 是 accuracy 口径下的
   大幅差异，且两口径方向一致）
4. 方法学观察：ECG5000 这类 146× 不平衡 + 小 train 的数据集，**整体 accuracy 是误导性指标**，
   报告必须附 per-class 指标——这本身是结题报告应写明的评估教训

---

## 三、2C 的核心科学结论

> **可学习向量化的表现受"数据量 × 任务拓扑相关性"双重制约。**
>
> - **数据量维度**：ECG200（100 样本）向量化层参数饥饿、CV 方差大、test 落后固定基线 7pp；
>   FordA（3601 样本）参数显著学习（σ 减半）、CV 方差降一个量级、差距缩至 1.6pp。
> - **任务拓扑相关性维度**（ECG5000 直接验证）：FordA（噪声，纯拓扑 50–62%）拓扑信息少，
>   可学习向量化无信息可挖；ECG5000（5 类心律，纯拓扑 87.5–89.8%）拓扑信息丰富，
>   **可学习向量化追平/略超固定基线**（混合 92.0% vs 固定融合 91.4%，+0.6pp，
>   5/5 种子一致，McNemar p=0.041；但 macro-F1 口径固定基线仍占优——不平衡数据的教训）。
> - **两个维度同时满足才可竞争**：ECG5000 既有足够样本（500 train，介于 ECG200 与 FordA 之间）
>   又有强拓扑相关性，是唯一可学习向量化占优（accuracy 口径）的场景；印证"数据量 × 拓扑相关性"双重门槛。
> - **一致的结论**：融合时频特征仍是关键（纯拓扑 < 融合，跨三数据集成立），
>   且**小数据 MLP 头不敌 RF**——最稳健的配置是"可学习向量化抽特征 + RF 分类"
>   （ECG200 混合 81%、FordA 混合 81.4%、ECG5000 混合 92.0%，均为各数据集可学习方案最佳）。

**与 2B 的呼应**：2B 说"稳定性≠判别力，固定向量化没有单一最优"；
2C 补上"可学习向量化的优势需要数据量 + 任务拓扑相关性双重门槛，且最优用法是表征+RF 而非端到端 MLP"。
两者共同构成向量化方法研究的完整证据链。

**对申报书创新点的表述建议**：
不宣称"可学习向量化普遍超越传统方法"，而是给出更有信息量的条件性结论——
**当数据充足且任务本身含拓扑信息时（如多类心律识别），可学习向量化（PersLay-lite）能自适应
学到优于手工的 PD 表征并反超固定基线；当任务判别信息不在拓扑域（如发动机噪声）或样本过少时，
手工特征 + RF 仍更优。可学习向量化的正确用法是作为表征器 + 传统分类器，而非端到端深度模型。**
这个结论有三个数据集的系统对比支撑、可复现，比单纯的"我们的方法更好"更有学术价值。

---

## 四、产物索引

| 类型 | 路径 |
|------|------|
| 可复用模块 | `src/perslay_lite.py`（PersLayLite + TopoClassifier + diagrams_to_padded） |
| 实验脚本 | `src/experiment_2c_ecg200_perslay.py`（首轮）<br>`src/experiment_2c_perslay_refine.py`（改进）<br>`src/experiment_2c_forda.py`（数据量检验）<br>`src/experiment_2c_ecg5000.py`（任务相关性检验，多分类） |
| 设计文档 | `docs/阶段2C_设计提纲.md` |
| 结果 | `results/exp2c_perslay/`（PD缓存、JSON、学习权重函数图、对比图）<br>`results/exp2c_forda/`（results_forda.json、forda_comparison.png）<br>`results/exp2c_ecg5000/`（results_ecg5000.json、ecg5000_comparison.png） |
| Commits | 0812415（首轮）→ c4705c7（改进）→ e1903b5（FordA）→ ECG5000（多分类反超） |

**关键图表**：
- `results/exp2c_perslay/learned_weight_fn.png`——学习到的权重函数 vs 2A 手工 30% 判据
- `results/exp2c_perslay/comparison_2c_refine.png`——首轮 vs 三个改进变体 vs 固定基线
- `results/exp2c_forda/forda_comparison.png`——FordA 五方案对比 + **数据量与可学习优势的关系图**
- `results/exp2c_ecg5000/ecg5000_comparison.png`——ECG5000 五方案对比 + **三数据集"任务拓扑相关性 vs 可学习优势"全景图**

---

## 五、遗留与可选后续

1. **未做的对照**：FordA/ECG5000 上未扫嵌入参数（沿用 m=5,τ=4）——若对各自任务微调嵌入参数，
   纯拓扑准确率可能再提升，但不影响"任务相关性决定可学习向量化能否反超"的核心结论
2. **MLP 头发散的修复**（可选）：ECG5000 上 PersLay+时频 MLP 融合发散，可尝试更低学习率/更大
   hidden/BN 层；但混合方案（PersLay 表征 + RF）已达 92.0% 且更稳定，端到端 MLP 非必需
3. **理论深化**：PersLay-lite 的 σ→0 极限退化为 PD 的点测度求和（与 Betti 曲线相关），
   σ→∞ 退化为全局平滑（与 PI 相关）——**可学习 σ 实际上在两种固定向量化之间连续插值**，
   这个观察可作为创新点的理论表述，值得与导师讨论

---

*阶段 2 全部完成：2A（时序表征 + ECG200 达 85%）→ 2B（向量化稳定性/连续性/判别力）→ 2C（可学习向量化：数据量检验 + 任务相关性检验，ECG5000 上反超固定基线）*
